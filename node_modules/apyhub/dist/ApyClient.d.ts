declare class ApyClient {
    protected readonly baseUrl = "https://api.apyhub.com";
    protected readonly headers: any;
    private readonly requestQueue;
    private readonly rateLimit;
    private readonly rateLimitPeriod;
    private rateLimitTimer;
    private requestPromises;
    protected debug: boolean;
    constructor(apyToken: string | {
        username: string;
        password: string;
    }, options?: {
        rateLimit: 1 | 2 | 3 | 4 | 5;
        debug: boolean;
    });
    private debugRequest;
    request(method: "get" | "post", url: string, data?: any, options?: any): Promise<any>;
    private startRateLimiter;
    private processRequestQueue;
    private handleResponse;
}
declare function getInstance(apyToken?: string | {
    username: string;
    password: string;
}, options?: {
    rateLimit: 1 | 2 | 3 | 4 | 5;
    debug: boolean;
}): ApyClient;
/**
 * Initializes the Apyhub API client.
 *
 * @param {string} apyToken - The Apyhub API token or Basic Authorization
 *   credentials.
 * @param {Object} [options] - Options for the API client.
 * @param {1 | 2 | 3 | 4 | 5} [options.rateLimit] - The rate limit for the API
 *   client.
 */
declare function initApyhub(apyToken: string | {
    username: string;
    password: string;
}, options?: {
    rateLimit: 1 | 2 | 3 | 4 | 5;
    debug: boolean;
}): void;
export { initApyhub, getInstance };
